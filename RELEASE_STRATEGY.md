# Fumi アプリ リリース品質化戦略

## 📦 プロジェクト概要

**アプリ名:** Fumi（筆まめのような宛名印刷アプリ）  
**技術:** Vanilla TypeScript + Tailwind CSS + jsPDF + Vite  
**現在の進捗:** 機能実装完了、リリース直前の品質調整段階  

### 実装済み機能
- ✅ CSV インポート
- ✅ 住所録管理（CRUD）
- ✅ 郵便番号から住所自動入力
- ✅ 宛名面印刷
- ✅ 裏面印刷（年賀状）
- ✅ PDF 出力
- ✅ 差出人情報管理
- ✅ 印刷位置キャリブレーション
- ✅ Undo/Redo
- ✅ 検索・フィルタリング
- ✅ 最近使った差出人（クイック選択）
- ✅ レスポンシブデザイン

---

## 🎯 リリース品質化の 3 段階計画

### **Phase 1: 基礎品質（1-2 日）**

リリース直前でのブロッカーと、ユーザーが困る UX 問題の修正。

#### 1.1 エラーハンドリング強化

| 項目 | 現状 | 改善内容 |
|------|------|--------|
| CSV インポート失敗 | 一般的なエラーメッセージ | ファイルフォーマットが不正か、容量超過かを明確化 |
| 郵便番号検索 API | タイムアウトなし | 3秒でタイムアウト + リトライボタン |
| ストレージエラー | エラーアラート | localStorage クォータ超過時のメッセージ + 古いデータ削除提案 |
| ネットワーク切断 | 検索が無応答 | オフラインモード表示 + 検索機能を無効化 |

#### 1.2 データバリデーション統一化

```
- 郵便番号: 7 桁数字のみ（ハイフンは自動フォーマット）
- 企業名: 1〜60 文字（現在 40 字制限を確認）
- 住所: 5〜200 文字（実装済み）
- 名前: 0〜50 文字
- 電話番号: 形式フリーだが、数字・ハイフン・括弧のみ許可
- 重複チェック: 郵便番号 + 企業名で完全重複を警告（許可するかは仕様次第）
```

#### 1.3 UX 改善（即座）

| No. | 問題 | 改善案 |
|-----|------|--------|
| 1 | CSV アップロード中はボタンが反応したまま | フォーム送信中は全ボタンを disabled に |
| 2 | 削除後の「データがありません」が急に出る | 削除後もリストを 1 秒キープしてから更新 |
| 3 | キーボード Cmd+S 送信失敗時に無応答 | フォーム validation エラーをトーストで通知 |
| 4 | モバイルでサイドバーが見づらい | モバイルでサイドバー非表示に（実装済みか確認） |
| 5 | 大量データのページングが遅い | ページサイズを 20 → 50 にしてみる（要テスト） |

---

### **Phase 2: ポーランド（3-4 日）**

機能は完成。あとは細部の完成度と信頼性。

#### 2.1 印刷品質チェック

**テスト項目:**
- [ ] 縦書き数字変換が全ケース正しいか
  - 例: `0123456789` → `〇一二三四五六七八九`
  - ハイフン、ハイフン代わりの「ー」も変換
- [ ] PDF 出力のマージン（上下左右 5mm）が実用的か
- [ ] キャリブレーション範囲（-15mm 〜 +15mm）で十分か
- [ ] フォントサイズが印刷時に適正か（相対サイズ vs 固定サイズ）
- [ ] 用紙サイズの自動判定（はがきサイズ）が macOS でも正しいか

**実装項目:**
- 印刷プレビュー表示（Chrome DevTools の印刷ビューで検証）
- A5 サイズ（はがき）の厳密なスタイル定義
- 郵便番号の枠線サイズ・位置を正確に

#### 2.2 レスポンシブ・アクセシビリティ

**デバイステスト:**
- [ ] iPhone SE（375px）での操作性
- [ ] iPad（768px）での 2 カラムレイアウト
- [ ] Mac（1280px 以上）での フル機能表示

**キーボード操作:**
- [ ] Tab キーで全ボタンにアクセス可
- [ ] Enter キーで フォーム送信（実装済みか確認）
- [ ] Escape キーでモーダル / フォーム閉じる（現状は未実装？）
- [ ] Cmd+Z / Cmd+Shift+Z で Undo/Redo（実装済み）

**アクセシビリティ:**
- [ ] ボタンに `aria-label` 追加
- [ ] フォームに `aria-required`, `aria-invalid` 追加
- [ ] カラーコントラスト比（WCAG AA）確認
  - ボタンテキスト vs 背景
  - ラベル vs 背景

#### 2.3 パフォーマンス

**ロードテスト:**
- [ ] 1000+ 件のデータでの読み込み時間（目標: 1秒以下）
- [ ] 検索フィルタリング時のレスポンス（目標: 200ms 以下）
- [ ] メモリ使用量（localStorage 上限 5-10MB の確認）

**最適化:**
- 必要に応じて 仮想スクロール実装（1000+ 件対応）
- ページネーション ページサイズ動的調整
- 画像圧縮（あれば）

---

### **Phase 3: 最終化（1-2 日）**

リリース前の最終チェック。

#### 3.1 ドキュメント / README

```markdown
# Fumi - 宛名印刷アプリ

## セットアップ
npm install
npm run dev

## 対応環境
- macOS 11 以上
- Chrome/Safari（最新版）
- 画面サイズ: 375px 以上

## 郵便番号検索 API について
- 外部 API（zipcloud）を使用
- 1 リクエスト 1 秒以上間隔
- 1 日のリクエスト数に制限あり

## よくある質問
- Q: データはどこに保存される？
  A: ブラウザの localStorage に保存
- Q: 他の PC にデータを移せる？
  A: CSV エクスポートで可能
```

#### 3.2 ビルド・デプロイ確認

- [ ] `npm run build` が エラーなく完了
- [ ] TypeScript 型チェックエラーなし（`tsc --noEmit`）
- [ ] dist フォルダが生成され、サイズが 200KB 以下
- [ ] dist/index.html が正常に開ける
- [ ] ビルド後も機能が全て動作するか

#### 3.3 最終テスト（シナリオテスト）

**シナリオ A: 初心者ユーザー**
1. アプリを開く
2. CSV ファイルをインポート
3. 差出人情報を設定
4. 宛名面を印刷
5. PDF として出力

**シナリオ B: 既存ユーザー（2回目）**
1. アプリを開く
2. 最近の差出人を選択
3. 新しいハガキを手動登録
4. 印刷位置をキャリブレーション
5. 裏面を印刷

**シナリオ C: エラー復旧**
1. 不正な CSV をアップロード（エラーメッセージ確認）
2. 不正な郵便番号を入力（検索失敗のハンドリング確認）
3. 削除後に Undo（復旧確認）
4. localStorage をクリア（リセット動作確認）

---

## 📝 タスク分解（実装順）

### Week 1

| 日 | タスク | 優先度 | 見積 |
|---|--------|--------|------|
| 1 | エラーハンドリング（CSV・郵便番号検索・ストレージ） | 🔴 高 | 4h |
| 2 | バリデーション統一化 | 🔴 高 | 3h |
| 3 | UX 改善（ボタン disabled・トースト） | 🟡 中 | 2h |
| 4 | 印刷品質テスト（実機）＆ 修正 | 🔴 高 | 6h |
| 5 | レスポンシブテスト（複数デバイス） | 🟡 中 | 3h |

### Week 2

| 日 | タスク | 優先度 | 見積 |
|---|--------|--------|------|
| 6 | キーボード操作・アクセシビリティ | 🟡 中 | 3h |
| 7 | パフォーマンステスト | 🟡 中 | 2h |
| 8 | README 作成 | 🟡 中 | 2h |
| 9 | ビルド確認 ＆ デプロイテスト | 🟡 中 | 2h |
| 10 | 最終シナリオテスト | 🟢 低 | 4h |

---

## 🚀 今週中にやるべき（今すぐ）

1. **開発サーバー起動 ＆ 実機テスト**
   ```bash
   npm install
   npm run dev
   ```
   → ブラウザで動作確認（Chrome DevTools でコンソールエラーも見る）

2. **TypeScript Strict Mode 有効化**
   ```json
   // tsconfig.json
   "strict": true
   ```
   → 型エラーがあれば修正

3. **エラーログを grep**
   ```bash
   grep -n "console.error\|Error\|catch" src/main.ts
   ```
   → ハンドルされていないエラーを特定

4. **印刷プレビュー**
   → Chrome DevTools の 印刷 → PDF で実際のレイアウトを確認

5. **CSV テストデータ作成**
   ```csv
   カテゴリ,企業名,名前,郵便番号,住所
   業務用,株式会社テスト,山田太郎,100-0001,東京都千代田区丸の内1-1-1
   ```
   → インポート ＆ 印刷で動作確認

---

## ✅ チェックリスト

### 基本機能
- [ ] CSV インポート（正常系 ＆ エラー系）
- [ ] 手動登録 ＆ 編集 ＆ 削除
- [ ] 郵便番号検索（成功系 ＆ 失敗系）
- [ ] 宛名面印刷
- [ ] 裏面印刷
- [ ] PDF 出力

### 高度な機能
- [ ] Undo / Redo
- [ ] 差出人クイック選択
- [ ] キャリブレーション
- [ ] 検索 ＆ フィルタリング

### 品質
- [ ] エラーハンドリング完全
- [ ] バリデーション統一
- [ ] UX 改善
- [ ] 印刷品質確認
- [ ] レスポンシブ確認
- [ ] アクセシビリティ対応
- [ ] パフォーマンス OK
- [ ] README 完成
- [ ] ビルド成功
- [ ] 最終テスト完了

---

## 💡 その他のアイデア（リリース後のアップデート）

- 🔄 **クラウド同期** (iCloud / Dropbox)
- 🎨 **複数テンプレート** (年賀状・挨拶状・招待状)
- 📊 **データ分析** (送信済み・未送信の区別)
- 🌍 **多言語対応** (英語など)
- 📱 **モバイルアプリ化** (React Native / Flutter)

---

**質問や追加したい項目があれば、いつでも言ってください。**
